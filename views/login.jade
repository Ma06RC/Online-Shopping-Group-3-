extends layout

block content
    h1= title

    h3= message

    form(action="/users/login", method="post")
        p username
            br
            input(type="text", name="username")
        p password
            br
            input(type="text", name="password")
        p
        input(type="submit")

